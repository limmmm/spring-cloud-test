server:
  port: 18082
  servlet:
    context-path: /springcache

spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 16379
    password: password
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
