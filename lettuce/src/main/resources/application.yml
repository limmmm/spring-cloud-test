server:
  port: 18088
  servlet:
    context-path: /lettuce

spring:
  application:
    name: lettuce
  redis:
    host: 127.0.0.1
    port: 16379
    password: password
    database: 1
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
